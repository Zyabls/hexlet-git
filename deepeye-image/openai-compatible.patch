diff --git a/ai_providers/openai_provider.py b/ai_providers/openai_provider.py
index b91fb97..af6414a 100644
--- a/ai_providers/openai_provider.py
+++ b/ai_providers/openai_provider.py
@@ -16,14 +16,24 @@ class OpenAIProvider:
         """Initialize OpenAI provider."""
         self.config = config
         self.api_key = config.get('api_key')
+        self.base_url = config.get('base_url')
         self.model = config.get('model', 'gpt-4o')
+        self.lock_model = bool(config.get('lock_model', False))
         self.temperature = config.get('temperature', 0.7)
+        self.top_p = config.get('top_p', 1.0)
         self.max_tokens = config.get('max_tokens', 2000)
+        self.timeout = config.get('timeout', 300)
         
         if not self.api_key:
             raise ValueError("OpenAI API key not provided")
         
-        self.client = OpenAI(api_key=self.api_key)
+        client_options = {
+            'api_key': self.api_key,
+            'timeout': self.timeout,
+        }
+        if self.base_url:
+            client_options['base_url'] = self.base_url.rstrip('/')
+        self.client = OpenAI(**client_options)
     
     def generate(self, prompt: str, **kwargs) -> str:
         """
@@ -37,8 +47,9 @@ class OpenAIProvider:
             Generated response
         """
         try:
+            requested_model = self.model if self.lock_model else kwargs.get('model', self.model)
             response = self.client.chat.completions.create(
-                model=kwargs.get('model', self.model),
+                model=requested_model,
                 messages=[
                     {
                         "role": "system",
@@ -50,10 +61,15 @@ class OpenAIProvider:
                     }
                 ],
                 temperature=kwargs.get('temperature', self.temperature),
+                top_p=kwargs.get('top_p', self.top_p),
                 max_tokens=kwargs.get('max_tokens', self.max_tokens)
             )
             
-            return response.choices[0].message.content
+            message = response.choices[0].message
+            content = message.content
+            if not content:
+                content = getattr(message, 'reasoning_content', None)
+            return content or ""
         
         except Exception as e:
             logger.error(f"OpenAI generation error: {e}")
diff --git a/tests/test_openai_compatible_provider.py b/tests/test_openai_compatible_provider.py
new file mode 100644
index 0000000..b9a10cb
--- /dev/null
+++ b/tests/test_openai_compatible_provider.py
@@ -0,0 +1,46 @@
+"""Regression coverage for the production OpenAI-compatible deployment path."""
+
+from types import SimpleNamespace
+from unittest.mock import MagicMock, patch
+
+from ai_providers.openai_provider import OpenAIProvider
+
+
+@patch("ai_providers.openai_provider.OpenAI")
+def test_custom_endpoint_timeout_and_model_lock(openai):
+    completion = MagicMock()
+    completion.choices = [MagicMock(message=MagicMock(content="ok"))]
+    openai.return_value.chat.completions.create.return_value = completion
+
+    provider = OpenAIProvider({
+        "api_key": "relay-managed-local-only",
+        "base_url": "http://127.0.0.1:18080/v1/",
+        "model": "gpt-oss-120b",
+        "lock_model": True,
+        "temperature": 1.0,
+        "top_p": 1.0,
+        "timeout": 300,
+        "max_tokens": 4096,
+    })
+
+    assert provider.generate("test", model="gpt-4o") == "ok"
+    openai.assert_called_once_with(
+        api_key="relay-managed-local-only",
+        base_url="http://127.0.0.1:18080/v1",
+        timeout=300,
+    )
+    request = openai.return_value.chat.completions.create.call_args.kwargs
+    assert request["model"] == "gpt-oss-120b"
+    assert request["temperature"] == 1.0
+    assert request["top_p"] == 1.0
+    assert request["max_tokens"] == 4096
+
+
+@patch("ai_providers.openai_provider.OpenAI")
+def test_reasoning_content_fallback(openai):
+    completion = SimpleNamespace(
+        choices=[SimpleNamespace(message=SimpleNamespace(content=None, reasoning_content="analysis result"))]
+    )
+    openai.return_value.chat.completions.create.return_value = completion
+    provider = OpenAIProvider({"api_key": "local", "model": "gpt-oss-120b"})
+    assert provider.generate("test") == "analysis result"
